{
    "name": "heroku-app",
    "version": "1.0.0",
    "engines": {
      "node": ">=14.0.0"
    },
    "scripts": {
      "start": "cd backend && node index.js",
      "heroku-postbuild": "cd backend && npm install && npm start"
    }
  }
  